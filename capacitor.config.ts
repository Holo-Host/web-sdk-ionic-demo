import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'web-sdk-ionic-demo',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
